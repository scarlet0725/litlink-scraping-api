version: '2'

services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - REDIS_ADDR=cache
      - REDIS_PORT=6379
  
  cache:
    image: redis:7.0-alpine
    ports:
      - "6379:6379"